describe("Home page functionality", () => {
    it("should display nav bar and the navbar links and buttons should function", () => {
        //navbar functionality and availability
    });

    it("should have a search input on nav and the search bar should work", () => {
        //search input
    });

    it("should contain a grid", () => {
        //test whether a grid is available
    });
});
